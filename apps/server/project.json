{
  "name": "server",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/server/src",
  "targets": {
    "build": {
      "cache": false,
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "pnpm exec esbuild --bundle --platform=node --target=es2022 --format=esm --outfile=dist/server/index.mjs --banner:js=\"import { createRequire } from 'module';const require = createRequire(import.meta.url);\" apps/server/src/index.ts",
          "rm -f dist/server/index.zip && zip -j dist/server/index.zip dist/server/index.mjs apps/server/assets/combined_aaguid.json"
        ],
        "cwd": "{workspaceRoot}",
        "parallel": false
      },
      "outputs": ["{workspaceRoot}/dist/server"]
    },
    "build-min": {
      "cache": false,
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "pnpm exec esbuild --bundle --minify --platform=node --target=es2022 --format=esm --outfile=dist/server/index.mjs --banner:js=\"import { createRequire } from 'module';const require = createRequire(import.meta.url);\" apps/server/src/index.ts",
          "rm -f dist/server/index.zip && zip -j dist/server/index.zip dist/server/index.mjs apps/server/assets/combined_aaguid.json"
        ],
        "cwd": "{workspaceRoot}",
        "parallel": false
      },
      "outputs": ["{workspaceRoot}/dist/server"]
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm exec vitest run --config apps/server/vitest.config.ts",
        "cwd": "{workspaceRoot}"
      }
    },
    "test-prod": {
      "executor": "nx:run-commands",
      "options": {
        "command": "QC_ENV=prod pnpm exec vitest run --config apps/server/vitest.config.ts",
        "cwd": "{workspaceRoot}"
      }
    }
  }
}
