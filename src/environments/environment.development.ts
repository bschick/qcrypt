import { version, copyright } from '../../package.json';

export const environment = {
    production: false,
    domain: 'https://qcrypt.schicks.net',
    version: version,
    copyright: copyright
};
