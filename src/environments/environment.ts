import copyright from '../../package.json';
import version from '../../package.json';

export const environment = {
    production: true,
    domain: 'https://quickcrypt.org',
    version: version,
    copyright: copyright
};